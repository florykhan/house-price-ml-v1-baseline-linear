# House Price Prediction ML

to work in the repo - use "source venv/bin/activate"

pip freeze, pip install -r

The src/ directory is reserved for reusable code, introduced in later versions (v2+). Data is empty
